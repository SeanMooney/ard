# MicroShift examples

These examples render MicroShift workload deployments.

## Examples

- [single-node/](single-node/) - one MicroShift VM with no additional workload
  services selected in the render file.

The MicroShift workload can be used directly, or as the base for OKO scenarios
that install OpenStack services on top of MicroShift.
