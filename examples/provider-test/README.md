# Provider test examples

These examples render provider-only smoke deployments. They are useful when you
want to validate provider rendering and lifecycle behavior without selecting a
full workload service profile.

## Examples

- [single-node/](single-node/) - one generic all-in-one node with no workload
  service profiles.
