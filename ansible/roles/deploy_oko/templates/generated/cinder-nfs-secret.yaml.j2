apiVersion: v1
kind: Secret
metadata:
  name: cinder-nfs-shares
  namespace: {{ oko_namespace }}
type: Opaque
stringData:
  nfs_shares: |
    {{ oko_cinder_nfs_share | default(oko_nfs_server ~ ':' ~ oko_nfs_export_path) }}
