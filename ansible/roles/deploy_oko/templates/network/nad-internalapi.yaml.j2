apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: internalapi
  namespace: {{ oko_namespace }}
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "internalapi",
      "type": "macvlan",
      "master": "{{ oko_internalapi_master }}",
      "ipam": {
        "type": "whereabouts",
        "range": "{{ oko_internalapi_cidr }}",
        "range_start": "{{ oko_internalapi_nad_start }}",
        "range_end": "{{ oko_internalapi_nad_end }}"
      }
    }
